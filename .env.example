OPENAI_API_KEY=sk-your-key-here

# Models
CHAT_MODEL=gpt-4o-mini
EMBED_MODEL=text-embedding-3-small
REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17
TTS_MODEL=tts-1
TTS_VOICE=alloy
STT_MODEL=whisper-1

# RAG
TOP_K=4
CHUNK_SIZE=800
CHUNK_OVERLAP=100

# Server
HOST=0.0.0.0
PORT=8000

# Access control (nobody can use the app until these are set)
ADMIN_KEY=change-me-long-random-string
ACCESS_CODES=alice-x7k2,bob-p9q1
MAX_CALL_SECONDS=300
DAILY_CALL_SECONDS=900
# Extra allowed browser origins (comma-separated); leave empty for same-origin only
ALLOWED_ORIGINS=
